[package]
name = "prediction-attestation"
version = "0.1.0"
description = "On-chain prediction attestations with Brier score calibration"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "prediction_attestation"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
